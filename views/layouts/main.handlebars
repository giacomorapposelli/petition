<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>Petition</title>
</head>
<header>
    {{#if login}}<a class="log-reg" href="/login">Log In</a>{{/if}}
    {{#if logout}}<a class="log-reg" href="/logout">Log Out</a>{{/if}}
    {{#if register}}<a class="log-reg" href="/register">Register</a>{{/if}}
    {{#if profile}}<a class="log-reg" href="/profile/edit">Edit Your Profile</a>{{/if}}

</header>

<body>
    <main>
        {{{body}}}
    </main>

    <footer>
        <p class="elrappo">© 2020 El Rappo</p>
    </footer>
</body>

</html>